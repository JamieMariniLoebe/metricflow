apiVersion: v1
kind: Service
metadata:
  name: grafana
  labels:
    app.kubernetes.io/name: metricflow
    app.kubernetes.io/component: dashboard
spec:
  selector:
    app.kubernetes.io/name: metricflow
    app.kubernetes.io/component: dashboard
  ports:
  - port: 3000
    targetPort: 3000
