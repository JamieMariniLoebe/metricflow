apiVersion: v1
kind: Service
metadata:
  name: prometheus
  labels:
    app.kubernetes.io/name: metricflow
    app.kubernetes.io/component: monitoring
spec:
  selector:
    app.kubernetes.io/name: metricflow
    app.kubernetes.io/component: monitoring
  ports:
  - port: 9090
    targetPort: 9090
